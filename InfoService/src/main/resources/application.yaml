spring:
  application:
    name: InfoService

server:
  port: 8082

eureka:
# instance:
#   hostname: localhost
  client:
    enable: true
    service-url:
      default-zone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
    instance:
      perfer-ip-address: true